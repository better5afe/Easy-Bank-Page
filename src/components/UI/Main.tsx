import AboutSection from '../subcomponents/main-sub/AboutSection';
import ArticlesSection from '../subcomponents/main-sub/ArticlesSection';

const Main = () => {
	return (
		<main>
			<AboutSection />
			<ArticlesSection />
		</main>
	);
};

export default Main;
